export default function HeroParaComp({ paragraph }) {
  return (
    <div className="hero-paragraph-container">
      <p className="hero-paragraph font-inter text-extralight">{paragraph}</p>
    </div>
  );
}
